import classNames from 'classnames/bind';

import styles from './ListBody.module.scss';

const cx = classNames.bind(styles);

const ListBody = () => {
  return <div className={cx('')}>ListBody</div>;
};

export default ListBody;
