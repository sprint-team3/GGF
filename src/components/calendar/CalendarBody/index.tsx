import classNames from 'classnames/bind';

import styles from './CalendarBody.module.scss';

const cx = classNames.bind(styles);

const CalendarBody = () => {
  return <div className={cx('')}>CalendarBody</div>;
};

export default CalendarBody;
